export enum Gender {
    Woman = 1,
    Man = 2,
}

export declare enum Language {
    Chinese = "zh-CN",
    English = "en-US",
    Japanese = "ja-JP",
    Korean = "ko-KR",
}

export enum ReadMode {
    Test = "test",
    Read = "read",
}
