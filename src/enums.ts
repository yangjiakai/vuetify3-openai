export enum Gender {
    Woman = 1,
    Man = 2,
}

export enum Language {
    Chinese = "zh-CN",
    English = "en-US",
    Japanese = "ja-JP",
    Korean = "ko-KR",
}

export enum ReadMode {
    Test = "test",
    Read = "read",
}

export enum ChatType {
    Base = "base",
    Creation = "creation",
    Consultant = "consultant",
    Spoken = "spoken"
} 